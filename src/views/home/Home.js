import Loading from "../../components/ui/Loading";

export default function Home() {
    const images = [
        "logo.svg",
    ];

    return <>
        <h1>Home Page</h1>
        {/*{images.map((el, i) => <img width={"100px"} key={i} src={require("../../assets/images/" + el)} alt=""/>)}*/}
    </>;
}